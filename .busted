return {
    _all = {
        coverage = true,
        verbose = true,
    },
    default = {
        directory = "src",
        lpath = "src/Vendor/?.lua",
        helper = "HeadlessWrapper.lua",
        ROOT = { "../spec" },
    }
}
