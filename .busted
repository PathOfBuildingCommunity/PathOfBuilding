return {
    _all = {
        coverage = true,
        verbose = true,
    },
    default = {
        directory = "src",
        lpath = "../bin/lua/?.lua",
        helper = "HeadlessWrapper.lua",
        ROOT = { "../spec" },
    }
}
